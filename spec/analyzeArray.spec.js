const expect = require('chai').expect;
const analyzeArray = require('../analyzeArray');

// describe.only('analzyArray', function() {
//     it('Should log the position of each element in the array', function () {
//         expect(analyzeArray(['carrot', ['car', 'boat', 'plane'], 'turtle', ['house']], 'Position')).to.equal(analyzeArray(['carrot', ['car', 'boat', 'plane'], 'turtle', ['house']], 'Position'));
//     });
// });
